# Chatbot mit Reverse Proxy

Dieses Setup stellt eine Docker-App (z. B. Streamlit-Chatbot) hinter einem Nginx-Reverse-Proxy bereit.

## Anforderungen

- Docker
- Domain: chatbot.firmenladen.de → zeigt auf Server-IP

## Start

```bash
docker compose up -d
